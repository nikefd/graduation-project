参考
［1］http://luokr.com/p/2
［2］http://www.litrin.net/2012/08/02/supervisor-%E8%BF%9B%E7%A8%8B%E6%8E%A7%E5%88%B6%E6%9C%8D%E5%8A%A1/

Supervisor安装和配置：
$ sudo easy_install supervisor
安装完毕后，生成Supervisor的配置文件并编辑：
$ echo_supervisord_conf > /etc/supervisord.conf
$ vim /etc/supervisord.conf
在文件末尾加入：
[program:hello]   //程序名称
command=python /var/www/index.py --port=8888 //执行路径
directory=/var/www 
autorestart=true //表示自启动
redirect_stderr=true 

可以参考Supervisor官方文档http://supervisord.org/。配置完毕后，我们就可以启动管理了：

$ supervisorctl start all -- 启动supervisor  所有服务

$ sudo supervisord //启动

supervisord.conf文件里会发现有一段[unix_http_server]的配置，默认是9001端口，去掉注释输入用户名和密码，重新启动，打开浏览器输入：http://localhost:9001

重启：直接ps -aux ｜grep supervisord
kill -9 pid
后启动

安装nginx

Nginx的配置，我们需要使用Nginx来将web请求代理到Tornado web server，新建配置文件：

$ vim /etc/nginx/conf.d/tornado.conf

websocket基本通信完成

前台嵌入ace编辑器





error:

import backports.ssl_match_hostname
ImportError: No module named backports.ssl_match_hostname

solution:
$ pip install backports.ssl_match_hostname

安装好Tornado后各种文件缺失
from tornado.concurrent import Future 
ImportError: No module named concurrent
等等

然后下载的demo也无法与服务器端进行交互

solution:下载最新的版本https://github.com/tornadoweb/tornado
然后将里面的tornado文件夹替换掉/usr/lib/python2.7/dist-packages里的tornado/

在server.py里设置static和template路径，

